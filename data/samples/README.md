# Controlled software-test inputs

`example_flows.csv` and `test_capture.pcap` are non-sensitive controlled inputs for verifying file parsing after real models have been trained. They are not a benchmark and must not be used to claim model performance.
